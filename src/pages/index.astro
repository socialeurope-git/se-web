---
import Listing from "../se/Listing.astro";
import Search from "../se/Search.astro";
const q = Astro.url.searchParams.get("s");
---
{q !== null && q.trim() ? <Search q={q} page={1} /> : <Listing kind="home" page={1} />}
